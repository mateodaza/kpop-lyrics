node_modules/
.next/
prisma/dev.db
prisma/dev.db-journal
.env
.env.local
.env*.local
*.log
.proof/

# Giveaway draw PII and candidate outreach files must never be committed.
*.private.csv
*.candidates.csv
giveaway-private/

# One-shot Chainlink draw build/runtime artifacts. Public proof JSON is tracked
# separately under public/giveaway/bts-2026/; local operator state is not.
chainlink/out/
chainlink/cache/
chainlink/broadcast/
chainlink/draw-state/
*.draw-state.json
*.keystore-password

# TS incremental build artifact — never commit
tsconfig.tsbuildinfo

# Mac-only lockfile breaks Railway linux build — keep untracked (Railway uses npm install)
package-lock.json
!staging/package-lock.json
.vercel/
